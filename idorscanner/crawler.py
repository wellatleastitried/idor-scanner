"""
Discovers endpoints:
    HTML links or forms
    JS artifact scraping
    Proxy capture mode (if running it behind mitmproxy is desired)

Produces an Endpoint Catalog (method, url_template, params)
"""
